{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "noEmit": true,
    "incremental": false,
    // Type-check the .mjs probe under the inherited strict settings.
    "checkJs": true,
    // Automatic @types inclusion does not kick in under inherited typeRoots.
    "types": ["node"]
  },
  "include": ["./fixture.mjs"]
}
