packages/agent-hooks/src/common/agent-hooks-common.ts#L96-L143 rk64:bcdde373158a6993
packages/git-span/src/span_root.rs#L30-L46 rk64:c82813a434bc42cf

TypeScript resolveSpanRoot mirrors the Rust CLI's resolve_span_root precedence (GIT_SPAN_DIR, then git config git-span.dir, then default .span) minus the --span-dir flag that file-write hooks cannot see, and isInsideSpanRoot reimplements the Rust containment predicate with a reversed (path, root) argument order.
