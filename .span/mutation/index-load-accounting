packages/git-span/tests/cases/add_index_loaded_once.rs#L1-L94 rk64:c8d22cdc6da50cf8

run_add threads one index snapshot through its mutation pipeline (the main-105 fix), and the post-write reconcile check's EngineState reads the index a fixed bounded number of times — two span reads plus one session-shared whole-file snapshot per resolution, zero per line-range anchor — so this fixture's total count is pinned at exactly 4, and a regression to per-anchor re-materialization fails the count.
