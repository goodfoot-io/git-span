.githooks/post-commit.git-span.sh#L8-L10 rk64:ad3c978db6fdcdb5
.githooks/post-rewrite.git-span.sh#L9-L11 rk64:ad3c978db6fdcdb5
packages/agent-hooks/package.json#L9-L9 rk64:161cd50131c89d0e

The post-commit and post-rewrite triggers locate the reconciler dispatcher at the bundle path the agent-hooks build writes, and their file-exists guard exits silently, so the trigger path and the build outfile must name the same artifact.
