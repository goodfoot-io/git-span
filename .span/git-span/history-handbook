packages/git-span/src/cli/history.rs#L1-L93 rk64:729c9f52cdbd60db
plugins-claude/git-span/skills/git-span/references/command-reference.md#L81-L100 rk64:ee858cc61536e131
plugins-codex/git-span/skills/git-span/references/command-reference.md#L81-L100 rk64:ee858cc61536e131

The handbook's history section documents the timeline the history command's module header defines — a newest-first git-log-style walk of span-declaration and per-anchor snapshot diffs that pairs anchors by content, then address, then similarity, rendering a re-anchor as a rename at or above the 50% similarity floor, as a deleted anchor plus a new anchor below it, and as declaration-asserted rename lines with no similarity claim when the recorded side cannot be read, renders unextractable content structurally rather than as prose, and leads with uncommitted worktree drift in human-text-default/schema-v2-JSON output — where the plugins-claude copy is normative and the plugins-codex copy mirrors it.
