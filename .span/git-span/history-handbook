packages/git-span/src/cli/history.rs#L1-L29 rk64:2bee894d93abe678
plugins-claude/git-span/skills/git-span/references/command-reference.md#L81-L87 rk64:3d806f34333f56cb
plugins-codex/git-span/skills/git-span/references/command-reference.md#L81-L87 rk64:3d806f34333f56cb

The handbook's history section documents the timeline the history command's module header defines — a newest-first git-log-style walk of span-declaration and per-anchor snapshot diffs that pairs anchors by content, then address, then similarity so re-anchors render as renames, renders unextractable content structurally rather than as prose, and leads with uncommitted worktree drift in human-text-default/schema-v2-JSON output — where the plugins-claude copy is normative and the plugins-codex copy mirrors it.
