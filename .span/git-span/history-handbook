packages/git-span/src/cli/history.rs#L1-L91 rk64:236e128a5f5f236c
plugins-claude/git-span/skills/git-span/references/command-reference.md#L81-L97 rk64:f965a19ec412004a
plugins-codex/git-span/skills/git-span/references/command-reference.md#L81-L97 rk64:f965a19ec412004a

The handbook's history section documents the timeline the history command's module header defines — a newest-first git-log-style walk of span-declaration and per-anchor snapshot diffs that pairs anchors by content, then address, then similarity, rendering a re-anchor as a rename only at or above the 50% similarity floor and as a deleted anchor plus a new anchor below it, renders unextractable content structurally rather than as prose, and leads with uncommitted worktree drift in human-text-default/schema-v2-JSON output — where the plugins-claude copy is normative and the plugins-codex copy mirrors it.
