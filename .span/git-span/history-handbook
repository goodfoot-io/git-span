packages/git-span/src/cli/history.rs#L1-L220 rk64:602b852fb0e40f66
plugins-claude/git-span/skills/git-span/references/command-reference.md#L81-L101 rk64:e5817b952a3a4934
plugins-codex/git-span/skills/git-span/references/command-reference.md#L81-L101 rk64:e5817b952a3a4934

The handbook's history section documents the timeline the history command's module header defines — a newest-first git-log-style walk of span-declaration and per-anchor snapshot diffs that walks merges as ordinary commits and diffs every entry against the state at its own first parent rather than against the entry printed above it, pairs anchors by content, then address, then similarity, rendering a re-anchor as a rename at or above the 50% similarity floor, as a deleted anchor plus a new anchor below it, and as declaration-asserted rename lines with no similarity claim when the recorded side cannot be read, renders unextractable content structurally rather than as prose, and leads with the span's live drift against its declaration across every layer git span stale reports — committed, staged, and uncommitted — each block naming its own layers on a drift source line, in human-text-default/schema-v2-JSON output — where the plugins-claude copy is normative and the plugins-codex copy mirrors it.
