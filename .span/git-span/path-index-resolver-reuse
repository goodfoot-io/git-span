packages/git-span/src/cli/show.rs#L527-L581 rk64:be292817de36dba8
packages/git-span/src/cli/stale_output.rs#L205-L278 rk64:22699b8fe2798e88
packages/git-span/src/span/read.rs#L333-L400 rk64:406e924d55c42e7b

Positional-arg resolution for `stale` and `show`/`list` builds one SpanPathIndex from a single corpus load and resolves every arg through it, so many args against a large span set stay O(args + spans) instead of reloading the corpus per arg.
