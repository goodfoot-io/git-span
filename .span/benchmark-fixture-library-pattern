packages/git-span/benches/stale_head_only.rs#L46-L68 rk64:fa3da495cdc9219e
packages/git-span/benches/stale_warm.rs#L58-L82 rk64:f262ed806945b964
packages/git-span/tests/support/mod.rs#L321-L368 rk64:8950a53f0f20281b

Benchmark fixtures use binary CLI (span_bin) to create test spans, but test support module uses library APIs (SpanFile::serialize). If SpanFile/AnchorRecord APIs change, bench fixtures fail at runtime; using library directly catches changes at compile time. Benchmarks and tests should use the same pattern.
