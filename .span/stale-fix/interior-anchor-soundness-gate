packages/git-span/src/cli/interior_anchor.rs#L95-L130 rk64:0b2f1176bc53b1d0
packages/git-span/src/cli/stale_output.rs#L712-L814 rk64:1c1d5e470c9c1cf7

Fail-closed gate that disables the stale --fix post-fix scoped splice and pre-fix source-layer reuse when any in-scope span carries an interior anchor, falling back to a baseline full re-resolve. The predicate and its run_stale consumer must agree on which optimizations the interior-anchor case bypasses.
