packages/git-span/src/cli/interior_anchor.rs#L95-L130 rk64:0b2f1176bc53b1d0
packages/git-span/src/cli/stale_output.rs#L532-L634 rk64:05d9acdfe912459d

The interior-anchor fail-closed gate is what stale --fix consults before choosing its post-fix path: when any in-scope span anchors a path under span_root, the scoped splice and pre-fix source-layer reuse are skipped in favor of a full re-resolve that matches the baseline byte-for-byte.
