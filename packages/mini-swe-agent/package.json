{
  "name": "mini-swe-agent",
  "version": "1.1.16",
  "description": "mini-swe-agent extension: subclasses upstream LocalEnvironment/DefaultAgent to bridge the git-span agent hooks (built from packages/agent-hooks into hooks/) into the agent loop",
  "license": "MIT",
  "scripts": {
    "build": "yarn build:hooks && yarn build:wheel",
    "build:hooks": "yarn workspace agent-hooks run build:hooks:mswea",
    "build:wheel": "uv build",
    "test": "UV_PROJECT_ENVIRONMENT=\"${GIT_SPAN_UV_PROJECT_ENVIRONMENT:-/var/cache/git-span/uv-envs/mini-swe-agent}\" uv run --group dev pytest -q",
    "lint": "UV_PROJECT_ENVIRONMENT=\"${GIT_SPAN_UV_PROJECT_ENVIRONMENT:-/var/cache/git-span/uv-envs/mini-swe-agent}\" uv run ruff check ."
  },
  "devDependencies": {
    "agent-hooks": "workspace:*"
  },
  "engines": {
    "node": ">=20.11.0"
  }
}
