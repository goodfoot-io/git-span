# Treatment arm config for the git-span x ProgramBench canary.
# All expected_* values are exact — preflight compares them exactly.
# Sourced from experiment/manifest.json (manifest_version 2);
# keep the two files in sync if either changes.
environment:
  hooks_enabled: true
  hooks_required: true
  hooks_dir: null
  node_bin: node
  hook_timeout_ms: 10000
  skill_file: /opt/git-span/skills/git-span/SKILL.md
  require_initial_no_spans: true
  experiment_arm: treatment

  expected_package_version: "1.1.4"
  expected_node_version: "v24.19.0"
  expected_git_span_version: "git-span 1.1.4"
  expected_bundle_sha256:
    static-plan.mjs: 0577b856b41a6b9bf5f869a4c746f45d7aa8a4069e6c7cbe02625e8c0875a700
    advisor.mjs: e00038973009cb27fdaebc7518c870ac58b482647456201635618d83fbe765fd
    post-tool-use.mjs: e554077a805effb43128386f512ab3612bae5eb90ad6ec48c7348a74f5b03082
    post-tool-use-failure.mjs: e859b774b818f93aecc0067f385cf5fe068979f5dff1c5c76432a0167583e2ea
    session-end.mjs: 47f353198497cd1c6755c155e7272f89ced28a633d6c4060cc534367ff000512
    # hooks.json is deliberately NOT listed: _build_attestation hashes only the
    # five ALL_HOOKS .mjs bundles, so an extra key here would compare against
    # None and fail preflight. Its sha256 (845463275087...) is pinned in
    # manifest.json instead.
  expected_skill_tree_sha256: c58380d15196f7952b2c65abd1488b7f774a2d1c92639cf9a1edfb6413a888dc

  # Informational only: programbench.py's process_instance() unconditionally
  # overwrites environment.image with f"{instance['image_name']}:task_cleanroom_v6"
  # for whichever instance is being run, so this value is never actually read
  # by the runner. Recorded here (and in manifest.json derived_image) so the
  # expected tag is visible without cross-referencing the manifest.
  image: "programbench/xorg62_1776_tty-clock.f2f847c:task_cleanroom_v6"

  # Override programbench.yaml's --cpus 20 --memory 60g: this devcontainer's
  # Docker Desktop VM only exposes 10 CPUs / ~27GB RAM (`docker info`), so the
  # upstream default fails outright ("range of CPUs is from 0.01 to 10.00").
  # Identical in treatment.yaml/control.yaml so it isn't an arm-level confound.
  run_args:
    - "--rm"
    - "--network"
    - "none"
    - "--cpus"
    - "8"
    - "--memory"
    - "20g"
    - "--memory-swap"
    - "20g"
    - "--user"
    - "agent"
    - "--cap-drop"
    - "SYS_PTRACE"

# Identical in treatment.yaml and control.yaml (guide: "Add the chosen model
# and provider configuration identically to both commands"). Requires
# DEEPSEEK_API_KEY in the runner's environment (export from DEEPSEEK_API_TOKEN
# in /workspace/.env.deepseek at launch).
model:
  model_name: deepseek/deepseek-v4-flash
