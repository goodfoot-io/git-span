[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"


[project]
name = "mini-swe-agent-git-span"
dynamic = ["version"]
description = "git-span agent hooks for mini-swe-agent: snapshot capture, span-debt advisory holds, and touch tracking, bridged from the Python agent loop over the Claude hook wire protocol"
readme = "README.md"
requires-python = ">=3.10"
license = "MIT"
license-files = ["LICENSE.md"]
keywords = ["nlp", "agents", "code", "git-span"]
dependencies = [
    "mini-swe-agent==2.4.6",
    "pydantic >= 2.0",  # v2 required for safe mutable default arguments (matches upstream)
]

[project.scripts]
mswea = "minisweagent_gitspan.cli:main"
mswea-extra = "minisweagent_gitspan.cli:main_extra"

[tool.setuptools]
include-package-data = true

[tool.setuptools.dynamic]
version = {attr = "minisweagent_gitspan.__version__"}

[tool.setuptools.packages.find]
where = ["src"]
include = ["minisweagent_gitspan*"]

[tool.setuptools.package-data]
minisweagent_gitspan = ["py.typed", "hooks/bin/*.mjs", "hooks/hooks.json"]

[tool.ruff]
line-length = 120
indent-width = 4

target-version = "py310"

[tool.ruff.lint]
select = [
    "E",   # pycodestyle errors
    "F",   # pyflakes
    "I",   # isort
    "UP",  # pyupgrade
    "B",   # bugbear
]
ignore = [
    "E501",  # line too long (upstream style)
    "E741",  # ambiguous variable name
]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
skip-magic-trailing-comma = false
line-ending = "auto"

[tool.pytest.ini_options]
markers = [
    "slow: marks tests as slow (deselect with '-k \"not slow\"')",
]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
    "ruff",
]
experiment = [
    "programbench==1.2.4",
]
